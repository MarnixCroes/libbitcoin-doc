## Windows libbitcoin-network build

On Windows, the MSBuild system is officially supported.

The build can be either a static debug build, or a static release build.
Both 32 and 64 bit are supported & tested.
